{ | modules, proxySpace, cleanup |
	//The server object attached to the proxySpace.
	var server = proxySpace.server;
	//Cleanup is a list that registers functions.
	//It is evaluated with the cleanup method.
	proxySpace.use({
		//Write your code here...
	});
}
